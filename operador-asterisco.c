#include <stdio.h>
#include <stdlib.h>

// operador asterisco usado de duas maneiras diferentes

int main()
{
	int x = 3, y = 5, z;
	z = y * x; // operador asterisco usado para multiplicação

	// operador asterisco para referenciar um ponteiro
	int *p;

	return 0;
}